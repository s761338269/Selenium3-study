PageObject设计模式核心要素

1.基础类
    基础类包含WebDriver实例的属性，如驱动的定义、对元素定位的二次封装
2.页面类
    每一个页面可(page)都继承基础类，并通过驱动来管理本页的元素，并将Page类中需要用的操作都封装成一个个方法。
3.用例层
    测试用例必须继承unittest.Tastcase类，并调用响相应的Page类来实现对应的测试步骤，最后是用例的断言。